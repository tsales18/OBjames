Para informar ao compilador que um numero desconhecido de parâmetros será passado para uma função, você deve terminar a declaração dos seus parâmetros usando três pontos

```
func(int a, int b, ...);
```
