Em C, uma ``union`` é uma posição de memoria que é compartilhada por duas ou mais variáveis diferentes, geralmente de tipos diferentes, em momentos diferentes. A definição de uma `union` é semelhante  à definição de estrutura:
```
union identificador {
	tipo nome_da_variável;
	tipo nome_da_variável;
	tipo nome_da_variável;

} variaveis_união;
```
Exemplo:
```
union u_type {
int i;
char ch;
};

union u_type cnvt;
```
Na `union cnvt`, tanto o inteiro ``i`` como o caractere `ch` compartilham a mesa posição de memória. (Obviamente, `i` ocupa `2 bytes` e `ch` usa apenas `1 bytes`)
![[Pasted image 20250801081825.png]]

para acessar um elemento da `union`, deve-se usar a mesma sintaxe que seria usada para estruturas:
```
cnvt.i = 10;

void func1(union u_type *un){
un->i = 10;
}
```
`unions` são usadas frequentemente quando conversões de tipo são necessárias, porque você pode referenciar os dados contidos na `union` de maneiras diferentes. Por exemplo, você pode usar uma `union` para manipular os bytes que constituem um double, a fim de alterar sua precisão ou para realizar um tipo de arredondamento incomum.
Contudo, usando uma ``union``, você pode criar facilmente uma função chamada `putw()` a qual escreve a representação binária de um inteiro em um arquivo, um byte por vez. 
```
union pw {
	int i;
	char ch[2];
};

putw(union pw word, FILE *fp) {
	putc(word->ch[0], fp)
	putc(word->ch[1], fp)
}
```