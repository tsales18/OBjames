
- **Default**: `std::thread t;` → não tem thread associada (não-joinable).
    
- **Com função**: `std::thread t(f, args...)` → começa a rodar `f(args...)` imediatamente.
    
- **Move**: `std::thread t(std::thread&& other)` → transfere a “propriedade” da thread.